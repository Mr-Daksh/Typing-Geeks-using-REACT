import React from "react";
import Feedback from "../feedback";
const Feedback = () => {
    return (
        <div>
            <h1>Feedback</h1>
        </div>
    );
};

export default Feedback;